# ============================================================
# PlacementAI — Environment Configuration
# Copy this file to .env and fill in your values
# ============================================================

# Flask
SECRET_KEY=shashank

# MySQL Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=shashank
DB_NAME=placement_db
DB_PORT=3306

# ============================================================
# Job Market Intelligence (Phase 5)
# ============================================================
# RemoteOK (the current data source, job_market.py) requires NO API key
# or signup -- nothing to configure here for it.
#
# Adzuna is documented as a POSSIBLE FUTURE data source (see
# docs/job-market-methodology.md) but is NOT implemented or called by
# this codebase. These are placeholders only, in case that decision is
# made later -- do not fill them in for the current RemoteOK-only setup.
# ADZUNA_APP_ID=
# ADZUNA_APP_KEY=
